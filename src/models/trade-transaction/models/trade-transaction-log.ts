export interface TxLog {
  identity: string;
  amount: string;
  asset: string;
  type: string;
  network: string;
  tx_fee: string;
  tx_kash_fee: string;
  tx_fee_denom: string;
  created_date: string;
}
